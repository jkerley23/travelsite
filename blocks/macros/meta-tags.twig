{#

Properties in the 'data' hash:
	title - Also affects OG tag.
	description - also affects OG tag.
	h1
	ogType
	ogUrl
	ogImage

None are required - defaults provided.

#}

{%- macro render(data) -%}
	{% set defaults = {
		title: "Travel inspiration and articles that will help you plan your next vacation. — MapQuest" ,
		description: "Get inspired to plan your next trip by disocovering  a hidden gem, a unique food tour or something completely unexpected. — MapQuest",
		keywords: "travel planning, travel, travel booking, travel articles, MapQuest",
		h1: "Travel Articles",
		ogType: "website",
		ogUrl: "https://travel.mapquest.com"
	} %}

	{% set data = defaults|merge(data) %}

	<meta name="title" content="{{ data.title }}" />
	<meta name="description" content="{{ data.description }}" />
	<meta name="H1" content="{{ data.h1 }}"/>
	<meta property="og:title" content="{{ data.title }}"/>
	<meta property="og:type" content="{{ data.ogType }}"/>
	<meta property="og:url" content="{{ data.ogUrl }}"/>
	{% if data.image is defined %}
		<meta property="og:image" content="{{ data.image }}" />
	{% endif %}
	<meta property="og:description" content="{{ data.description }}" />
{%- endmacro -%}
